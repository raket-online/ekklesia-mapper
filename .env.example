# Environment Variables for Ekklesia Mapper

# --- Application Configuration ---
# NODE_ENV: Set to 'development' for development, 'production' for production.
NODE_ENV=development
# PORT: The port the Express server will listen on.
PORT=5173

# --- Database Configuration (Neon / Vercel Postgres) ---
# POSTGRES_URL: Connection string for your PostgreSQL database.
# Example: postgresql://user:password@host:port/database?sslmode=require
POSTGRES_URL="your_postgres_connection_string_here"
# DATABASE_URL: Alternative connection string for your PostgreSQL database.
# Often used for Vercel deployments.
DATABASE_URL="your_database_connection_string_here"

# --- Authentication Configuration (Beter Auth) ---
# AUTH_SECRET: A secret key used for signing authentication tokens.
# Generate a strong, random string (e.g., openssl rand -base64 32).
AUTH_SECRET="your_auth_secret_key_here"
# BETTER_AUTH_SECRET: A secret key used by the better-auth library for signing.
# Should be the same as AUTH_SECRET if better-auth uses this directly.
BETTER_AUTH_SECRET="your_better_auth_secret_key_here"

# --- App URLs (for local development) ---
# BETTER_AUTH_URL: The base URL for the authentication endpoints.
# This should match your application's public URL in production.
BETTER_AUTH_URL="http://localhost:5173"
# BETTER_AUTH_APP_URL: The base URL of your application.
# This should match your application's public URL in production.
BETTER_AUTH_APP_URL="http://localhost:5173"
